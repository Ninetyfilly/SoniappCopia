utils, por lo general lo uso para archivos de javascript que contengan funciones,
por ejemplo, funciones que den formato a un texto, que traduzcan textos, entre otras cosas.

Un ejemplo podría ser una función que formatee una fecha, así cada que lo queramos hacer en algún componente, llamaremos a esta función en vez de escribir el
código una y otra vez